        <!-- Footer -->
        <div class = "container">
        <footer>
            <div class="row">
                <div class="col-lg-12">
                <% if current_user %>
                    <p>Copyright &copy; <%= current_user.first_name + " " + current_user.last_name + " " + Time.now.year.to_s %></p>
                  <% end %>
                </div>
            </div>
            <!-- /.row -->
        </footer>
        </div>